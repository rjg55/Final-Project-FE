import React from "react";
import GroupListItem from "./GroupListItem";

const GroupListGenerator = ({ groupList }) => {
  return {
    //This is where we need to map a list (do we want a scroll view??)
  };
};

export default GroupListGenerator;
