import { View, Text } from 'react-native';
import EventList from '../components/EventList';

const Events = () => {
  return (
    <View>
      <EventList />
    </View>
  );
};

export default Events;
